# ═══════════════════════════════════════════════════════════════
# WHATSAPP BOT CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and customize the values
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# BOT SETTINGS
# ───────────────────────────────────────────────────────────────

# Command prefix - the character(s) that trigger commands
# Default: !
BOT_PREFIX=!

# Bot display name - used in help messages and info
# Default: WhatsAppBot
BOT_NAME=WhatsAppBot

# Owner's phone number (with country code, no + or spaces)
# This number has access to owner-only commands (ban, broadcast, etc.)
# Example: 1234567890 for +1 234 567 890
OWNER_NUMBER=

# ───────────────────────────────────────────────────────────────
# ANTI-BAN CONFIGURATION - Rate Limiting
# ───────────────────────────────────────────────────────────────

# Global rate limit - maximum messages per minute across all chats
# Recommended: 20-40 for personal use, 10-20 for safety
# Default: 30
RATE_LIMIT_PER_MINUTE=30

# Per-user rate limit - max messages per minute to single user
# Default: 5
RATE_LIMIT_PER_USER=5

# Per-group rate limit - max messages per minute to single group
# Default: 10
RATE_LIMIT_PER_GROUP=10

# Block duration when rate limit exceeded (in minutes)
# User/group is blocked for this duration after hitting limit
# Default: 5
RATE_LIMIT_BLOCK_DURATION=5

# ───────────────────────────────────────────────────────────────
# ANTI-BAN CONFIGURATION - Response Delays
# ───────────────────────────────────────────────────────────────

# Minimum delay before responding (milliseconds)
# Makes bot appear more human-like
# Recommended: 1000-3000
# Default: 2000
MIN_RESPONSE_DELAY_MS=2000

# Maximum delay before responding (milliseconds)
# Actual delay is random between min and max
# Recommended: 3000-6000
# Default: 5000
MAX_RESPONSE_DELAY_MS=5000

# Typing speed simulation (milliseconds per character)
# Higher = slower, more realistic typing
# Recommended: 50-100
# Default: 75
TYPING_SPEED_MS=75

# Maximum typing duration (milliseconds)
# Caps the typing indicator duration for long messages
# Default: 8000
MAX_TYPING_DURATION_MS=8000

# ───────────────────────────────────────────────────────────────
# ANTI-SPAM CONFIGURATION
# ───────────────────────────────────────────────────────────────

# Enable duplicate message detection
# Prevents sending identical messages repeatedly
# Default: true
ENABLE_DUPLICATE_DETECTION=true

# Time window for duplicate detection (seconds)
# Messages within this window are checked for duplicates
# Default: 60
DUPLICATE_WINDOW_SECONDS=60

# Similarity threshold for near-duplicate detection (0.0-1.0)
# Messages with similarity above this are considered duplicates
# 0.85 = 85% similar
# Default: 0.85
SIMILARITY_THRESHOLD=0.85

# ───────────────────────────────────────────────────────────────
# FEATURES
# ───────────────────────────────────────────────────────────────

# Enable group message handling
# Set to false to only respond in private chats
# Default: true
ENABLE_GROUPS=true

# Automatically mark incoming messages as read
# Default: true
ENABLE_AUTO_READ=true

# Show typing indicator before responding
# Highly recommended for anti-ban
# Default: true
ENABLE_TYPING_INDICATOR=true

# Enable periodic presence updates (online status)
# Keeps the bot appearing online
# Default: true
ENABLE_PRESENCE_UPDATES=true

# Presence update interval (minutes)
# How often to send "online" status
# Default: 5
PRESENCE_UPDATE_INTERVAL=5

# ───────────────────────────────────────────────────────────────
# MESSAGE QUEUE
# ───────────────────────────────────────────────────────────────

# Maximum retry attempts for failed messages
# Default: 3
QUEUE_MAX_RETRIES=3

# Delay between retry attempts (milliseconds)
# Default: 5000
QUEUE_RETRY_DELAY=5000

# Minimum delay between queued messages (milliseconds)
# Default: 1000
QUEUE_MIN_DELAY=1000

# Maximum delay between queued messages (milliseconds)
# Default: 3000
QUEUE_MAX_DELAY=3000

# ───────────────────────────────────────────────────────────────
# PATHS
# ───────────────────────────────────────────────────────────────

# Session/auth data storage location
# Default: ./data/auth
AUTH_FOLDER=./data/auth

# Log files directory
# Default: ./logs
LOG_FOLDER=./logs

# ───────────────────────────────────────────────────────────────
# LOGGING
# ───────────────────────────────────────────────────────────────

# Log level: trace, debug, info, warn, error, fatal
# - trace: Most verbose, shows everything
# - debug: Debug info + above
# - info: General info + above (recommended)
# - warn: Warnings + above
# - error: Errors + above
# - fatal: Only critical errors
# Default: info
LOG_LEVEL=info

# Enable pretty printing for logs
# Set to false in production for JSON logs
# Default: true
LOG_PRETTY=true

# ───────────────────────────────────────────────────────────────
# RECONNECTION
# ───────────────────────────────────────────────────────────────

# Maximum reconnection attempts before giving up
# Default: 10
MAX_RECONNECT_ATTEMPTS=10

# ═══════════════════════════════════════════════════════════════
# END OF CONFIGURATION
# ═══════════════════════════════════════════════════════════════
